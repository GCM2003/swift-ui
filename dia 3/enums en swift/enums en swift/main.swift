// este código tiene como objetivo definir y explicar qué es un enum en swift

import Foundation

// los enum son un tipo de dato en swift y los valores que puede tomar los define el programador
// ejemplo:
enum Semana{
    case lunes
    case martes
    case miercoles
    case viernes
    case sábado
    case domingo
}
